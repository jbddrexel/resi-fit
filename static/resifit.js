document.addEventListener('DOMContentLoaded', function() {
    var elem = document.getElementById('fv_calc_focal_point')
    if (elem) {
        elem.scrollIntoView();
    }
    var elem = document.getElementById('pmt_calc_focal_point')
    if (elem) {
        elem.scrollIntoView();
    }
})
